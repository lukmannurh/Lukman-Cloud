import * as React from 'react';
import { CheckCircle2, AlertCircle, Info, XCircle } from 'lucide-react'; export interface ToastProps extends React.HTMLAttributes<HTMLDivElement> { title: string; description?: string; variant?: 'default' | 'success' | 'warning' | 'danger'; onAction?: () => void; actionLabel?: string;
} export function Toast({ className = '', title, description, variant = 'default', onAction, actionLabel, ...props }: ToastProps) { return ( <div className={`fixed top-4 left-1/2 -translate-x-1/2 md:top-auto md:bottom-4 md:left-auto md:right-4 md:translate-x-0 z-[9999] flex w-full max-w-sm flex-col gap-1 rounded-lg border border-border bg-surface p-4 shadow-lg transition-all duration-[--dur] ${className}`.trim()} {...props} > <div className="flex items-start gap-3"> {variant === 'success' && <CheckCircle2 className="h-5 w-5 text-success mt-0.5" />} {variant === 'warning' && <AlertCircle className="h-5 w-5 text-warning mt-0.5" />} {variant === 'danger' && <XCircle className="h-5 w-5 text-danger mt-0.5" />} {variant === 'default' && <Info className="h-5 w-5 text-muted-foreground mt-0.5" />} <div className="flex-1 grid gap-1"> <p className="text-sm font-semibold text-foreground leading-tight"> {title} </p> {description && ( <p className="text-sm text-muted-foreground leading-tight"> {description} </p> )} </div> {onAction && actionLabel && ( <button onClick={onAction} className="text-xs font-medium text-primary hover:text-primary-hover focus:outline-none focus-visible:underline shrink-0" > {actionLabel} </button> )} </div> </div> );
}
